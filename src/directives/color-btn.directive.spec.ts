import { ColorBtnDirective } from './color-btn.directive';

describe('ColorBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBtnDirective();
    expect(directive).toBeTruthy();
  });
});
